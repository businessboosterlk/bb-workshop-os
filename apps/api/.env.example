# Workshop OS API. Copy to .env.local for dev; on Railway set these as Variables.
DATA_MODE=supabase
SUPABASE_URL=https://yyviiwnqgphyklcoijyd.supabase.co
SUPABASE_SERVICE_ROLE_KEY=            # server side only. never in a browser, never in git
WOS_SECRET=                           # long random string; signs every session token
WOS_ORIGINS=https://businessboosterlk.github.io
WOS_DEMO_CODE=                        # memory mode only: a code that always opens the customer door
